/// The four rows of the Home pre-arrival checklist (Figma `2237:4237`).
///
/// `contactDetails` is seeded complete, which is why Home opens at 1/4.
enum PreArrivalStep { contactDetails, identity, arrivalTime, specialRequests }
